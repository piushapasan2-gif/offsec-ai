# Minimal — no heavy SDKs, all APIs called via raw HTTP
Flask>=3.0.0
flask-socketio>=5.3.6
python-socketio>=5.10.0
python-engineio>=4.8.0
python-dotenv>=1.0.0
requests>=2.31.0
cryptography>=42.0.0

# Production WSGI (gthread = robust, no monkey-patching needed)
gunicorn>=21.2.0
